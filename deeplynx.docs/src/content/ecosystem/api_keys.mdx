import Image from 'next/image'


# API Keys

API authentication requires you to obtain a temporary Bearer token, create your API Key and Secret, then exchange them for access tokens with configurable lifetimes for your API requests.

## Authentication Workflow

### Step 1: Sign into the Application

Sign into the app through the web interface to access your account.

### Step 2: Create Your API Key and Secret

Select the profile icon in the bottom left corner and click the "settings" button:

<Image
  src="/docs/images/user-settings.png"
  alt="Create Project UI"
  width={800}
  height={600}
/>

Click "Generate New" in the API Keys section at the bottom of the settings page:

<Image
  src="/docs/images/generate-api-key.png"
  alt="Create Project UI"
  width={800}
  height={600}
/>

A new API key/secret will show up in the top banner:

<Image
  src="/docs/images/key-generated.png"
  alt="Create Project UI"
  width={800}
  height={600}
/>

> **Critical**: Your API Secret will only be shown once. Save both the API Key and Secret immediately in a secure location (password manager, environment variables, secrets manager). Never commit these to version control.

### Step 3: Create Access Tokens

Now use your API Key and Secret to generate access tokens with your desired expiration time. This endpoint does not require a bearer token.

#### Endpoint

```
POST /api/v1/token/CreateToken
```

#### Request Body

```json copy
{
  "apiKey": "ak_live_abc123...",
  "apiSecret": "sk_live_xyz789...",
  "expirationMinutes": 60
}
```

The `expirationMinutes` parameter is optional and sets the token lifetime in minutes. If omitted, tokens default to 60 minutes.

#### Example Request

```bash copy
curl /api/v1/token/CreateToken \
  --request POST \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer [temporary-token-from-step-2]' \
  --data '{
    "apiKey": "ak_live_abc123...",
    "apiSecret": "sk_live_xyz789...",
    "expirationMinutes": 60
  }'
```

#### Response

```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "expiresIn": 3600
}
```

### Step 4: Use Access Tokens for API Requests

Include your access token in the Authorization header of all API requests:

#### Example Request

```bash copy
curl /api/v1/projects/GetAllProjects \
  --request GET \
  --header "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

### Step 5: Refresh Expired Tokens

When your access token expires, generate a new one by calling CreateToken again with your saved API Key and Secret. You no longer need the temporary Bearer token from Step 2 - just your API credentials.

```bash copy
curl /api/v1/token/CreateToken \
  --request POST \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer [your-current-valid-token]' \
  --data '{
    "apiKey": "ak_live_abc123...",
    "apiSecret": "sk_live_xyz789...",
    "expirationMinutes": 120
  }'
```

## Need Help?

If you need help or have any questions, feel free to reach out to us through our [support channels](/about/aboutDe). We're here to help!

Thank you for being a part of our community. We hope you find the documentation helpful and look forward to your feedback!
